PORT=5000

# Either set DATABASE_URL, or the individual PG* vars below.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/rhaq

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=rhaq

JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d
